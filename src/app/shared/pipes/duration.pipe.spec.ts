import { DurationPipePipe } from './duration.pipe';

describe('DurationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
